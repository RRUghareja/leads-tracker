# Copy to .env to override defaults. Every value below is optional.
NODE_ENV=development
PORT=4000
DATABASE_PATH=data/leads.db
CORS_ORIGIN=http://localhost:3000

# Leave both empty to disable authentication. Set both to protect every /api route
# (except /api/health) with HTTP Basic auth.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
